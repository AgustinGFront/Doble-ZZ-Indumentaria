{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm install && vite build"
  },
  "dependencies": {
    "firebase": "^12.5.0"
  },
  "devDependencies": {
    "vite": "^7.2.2"
  }
}